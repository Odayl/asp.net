@model IEnumerable<ProjetFinal.Models.Food>

@{
    ViewData["Title"] = "FoodsAndTheirChefs";
}

<h1>FoodsAndTheirChefs</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Specialite)
            </th>
<th>
                Chef Name
            </th>
            <th>Chef Nat</th>
                    <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialite)
            </td>
            <td>
                    @Html.DisplayFor(modelItem =>  item.Chef.Name)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Chef.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Chef.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
